package gateway

import "github.com/synapse-chat/synapse/pkg/wire"

// QUICALPN is the ALPN token negotiated for the Synapse QUIC transport
// (re-exported from wire for gateway callers).
const QUICALPN = wire.QUICALPN
